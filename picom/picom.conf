backend = "glx";
vsync = true;

# === Скругление углов ===
corner-radius = 12;
rounded-corners-exclude = [
  "class_g = 'i3bar'",
  "class_g = 'Polybar'"
];

# === Прозрачность (пример) ===
inactive-opacity = 0.95;
opacity-rule = [
  "95:class_g = 'kitty'",
  "100:class_g = 'kitty' && focused"
];

